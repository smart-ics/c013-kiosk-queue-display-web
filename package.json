{
  "name": "c013-kiosk-queue-display-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.14.0",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev:kiosk": "pnpm --filter kiosk-web dev",
    "dev:display": "pnpm --filter display-web dev",
    "dev:config": "pnpm --filter config-web dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "~5.9.0"
  }
}
