.../src/composables/useDisplayScreenList.ts        | 38 +++++++-------------
 apps/kiosk-web/src/composables/useStationList.ts   | 38 +++++++-------------
 packages/device-config/src/deviceList.ts           | 42 ++++++++++++++++++++++
 packages/device-config/src/index.ts                |  1 +
 4 files changed, 67 insertions(+), 52 deletions(-)

--- Changes ---

apps/display-web/src/composables/useDisplayScreenList.ts
  @@ -1,7 +1,8 @@
  +import { createListFetcher, type DeviceListStatus } from '@aq/device-config'
   import { getDeviceConfigProvider } from '../infrastructure'
   
  -export type DisplayScreenListStatus = 'idle' | 'loading' | 'ok' | 'error'
  +export type DisplayScreenListStatus = DeviceListStatus
   
   export interface DisplayScreenListState {
     status: Ref<DisplayScreenListStatus>
  @@ -15,10 +16,9 @@ const FALLBACK_ERROR = 'Gagal memuat daftar screen.'
  -  const status = ref<DisplayScreenListStatus>('idle')
  +  const status = ref<DisplayScreenListStatus>('loading')
     const screenIds = ref<string[]>([])
     const error = ref<string | null>(null)
  -  let loadId = 0
   
     const defaultFetch = async (): Promise<string[]> => {
       const provider = await getDeviceConfigProvider()
  @@ -27,36 +27,22 @@ export function useDisplayScreenList(opts?: {
  -  const run = (): void => {
  -    const myId = ++loadId
  -    status.value = 'loading'
  -    error.value = null
  -    fetchImpl()
  -      .then((ids) => {
  -        if (myId !== loadId) return
  -        screenIds.value = ids
  -        status.value = 'ok'
  -      })
  -      .catch((err: unknown) => {
  -        if (myId !== loadId) return
  -        error.value = err instanceof Error && err.message ? err.message : FALLBACK_ERROR
  -        screenIds.value = []
  -        status.value = 'error'
  -      })
  -  }
  -
  -  run()
  +  const fetcher = createListFetcher(fetchImpl, FALLBACK_ERROR, (s) => {
  +    status.value = s.status
  +    screenIds.value = s.items
  +    error.value = s.error
  +  })
   
     if (getCurrentScope()) {
  -    onScopeDispose(() => {
  -      loadId += 1
  -    })
  +    onScopeDispose(() => fetcher.cancel())
     }
   
  +  fetcher.run()
  +
     return {
       status,
       screenIds,
       error,
  -    refresh: run,
  +    refresh: () => fetcher.run(),
     }
   }
  +12 -26

apps/kiosk-web/src/composables/useStationList.ts
  @@ -1,7 +1,8 @@
  +import { createListFetcher, type DeviceListStatus } from '@aq/device-config'
   import { getDeviceConfigProvider } from '../infrastructure'
   
  -export type KioskStationListStatus = 'idle' | 'loading' | 'ok' | 'error'
  +export type KioskStationListStatus = DeviceListStatus
   
   export interface KioskStationListState {
     status: Ref<KioskStationListStatus>
  @@ -15,10 +16,9 @@ const FALLBACK_ERROR = 'Gagal memuat daftar kiosk station.'
  -  const status = ref<KioskStationListStatus>('idle')
  +  const status = ref<KioskStationListStatus>('loading')
     const stationIds = ref<string[]>([])
     const error = ref<string | null>(null)
  -  let loadId = 0
   
     const defaultFetch = async (): Promise<string[]> => {
       const provider = await getDeviceConfigProvider()
  @@ -27,36 +27,22 @@ export function useStationList(opts?: {
  -  const run = (): void => {
  -    const myId = ++loadId
  -    status.value = 'loading'
  -    error.value = null
  -    fetchImpl()
  -      .then((ids) => {
  -        if (myId !== loadId) return
  -        stationIds.value = ids
  -        status.value = 'ok'
  -      })
  -      .catch((err: unknown) => {
  -        if (myId !== loadId) return
  -        error.value = err instanceof Error && err.message ? err.message : FALLBACK_ERROR
  -        stationIds.value = []
  -        status.value = 'error'
  -      })
  -  }
  -
  -  run()
  +  const fetcher = createListFetcher(fetchImpl, FALLBACK_ERROR, (s) => {
  +    status.value = s.status
  +    stationIds.value = s.items
  +    error.value = s.error
  +  })
   
     if (getCurrentScope()) {
  -    onScopeDispose(() => {
  -      loadId += 1
  -    })
  +    onScopeDispose(() => fetcher.cancel())
     }
   
  +  fetcher.run()
  +
     return {
       status,
       stationIds,
       error,
  -    refresh: run,
  +    refresh: () => fetcher.run(),
     }
   }
  +12 -26

packages/device-config/src/deviceList.ts
  @@ -0,0 +1,42 @@
  +export type DeviceListStatus = 'loading' | 'ok' | 'error'
  +
  +export type DeviceListState = {
  +  status: DeviceListStatus
  +  items: string[]
  +  error: string | null
  +}
  +
  +export type DeviceListFetcher = {
  +  run: () => void
  +  cancel: () => void
  +}
  +
  +export function createListFetcher(
  +  fetchImpl: () => Promise<string[]>,
  +  fallbackMsg: string,
  +  onUpdate: (state: DeviceListState) => void,
  +): DeviceListFetcher {
  +  let loadId = 0
  +
  +  const isStale = (myId: number) => myId !== loadId
  +
  +  const run = () => {
  +    const myId = ++loadId
  +    onUpdate({ status: 'loading', items: [], error: null })
  +    fetchImpl()
  +      .then((items) => {
  +        if (isStale(myId)) return
  +        onUpdate({ status: 'ok', items, error: null })
  +      })
  +      .catch((err: unknown) => {
  +        if (isStale(myId)) return
  +        const msg = err instanceof Error && err.message ? err.message : fallbackMsg
  +        onUpdate({ status: 'error', items: [], error: msg })
  +      })
  +  }
  +
  +  return {
  +    run,
  +    cancel: () => { loadId += 1 },
  +  }
  +}
  +42 -0

packages/device-config/src/index.ts
  @@ -10,3 +10,4 @@ export {
  +export * from './deviceList'
  +1 -0
