# display-web

Stub app for Admission Queue Display (Phase C3).

Path: `/display/{screenId}`

C2.0 only scaffolds Vite base, SPA `web.config`, and `version.json`. Snapshot + SignalR come in C3.0 / C3.1.
